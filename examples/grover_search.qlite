# Grover's Search for 2 Qubits (Target state |11>)
qubit q[2];

# 1. Initialization (Superposition)
H q[0];
H q[1];

# 2. Oracle (Flips the sign of |11>)
# For 2 qubits, a CZ gate acts as the oracle for |11>
CZ(q[0], q[1]);

# 3. Diffusion Operator (Amplify the target)
H q[0];
H q[1];
X q[0];
X q[1];
CZ(q[0], q[1]);
X q[0];
X q[1];
H q[0];
H q[1];
